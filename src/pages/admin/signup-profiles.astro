---
// Admin Portal — Sign Up Profiles
// Approve/reject actions not ported — list only.
import AdminLayout from '../../layouts/AdminLayout.astro';
import Card from '../../components/ui/Card';
import Table from '../../components/ui/Table';
import { checkAuth } from '../../lib/auth';
import { createServerSupabase } from '../../lib/supabase';

const auth = await checkAuth(Astro.cookies, ['super_admin', 'admin', 'proprietor', 'head_teacher', 'principal']);

if (auth.status === 'unauthenticated') {
  return Astro.redirect(`/login?next=${encodeURIComponent(Astro.url.pathname)}`);
}
if (auth.status === 'unauthorized') {
  return Astro.redirect('/403');
}

const supabase = createServerSupabase(Astro.cookies);
const { data: rows, error } = await supabase
  .from('signup_requests')
  .select('*')
  .limit(25);

const columns = [
  { key: 'full_name', header: 'Name' },
  { key: 'status', header: 'Status' }
];
---
<AdminLayout title="Sign Up Profiles" activePath="/admin/signup-profiles" role={auth.role}>
  <Card title="Sign Up Profiles">
    {error && (
      <p class="text-sm text-red-600 mb-3">
        Query against `signup_requests` failed — table/columns not verified against the live
        schema yet (same as-audited-not-verified caveat as Phase 0's table list).
      </p>
    )}
    <Table
      columns={columns}
      rows={(rows ?? []) as any[]}
      getRowKey={(row: any) => String(row.id ?? JSON.stringify(row))}
    />
  </Card>
</AdminLayout>
